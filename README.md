# gnn-for-energy-prediction
Graph neural network that predicts the internal energy of a molecule in eV given its structure. The model was trained on the QM9 dataset, which contains ~130k conformations of different molecules.
