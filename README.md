# gnn-for-energy-prediction
Graph neural network that predicts the internal energy of a molecule in eV given its structure. Additional functionality is included to convert SMILES string to a format the model can use. 
